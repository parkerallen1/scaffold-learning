# Copy to functions/.secret.local for emulator-compatible secret loading.
# The emulator always uses the deterministic FakeAiProvider and does not read OPENAI_API_KEY.
OPENAI_API_KEY=
STUDENT_PIN_PEPPER=
