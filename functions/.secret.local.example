# Copy to functions/.secret.local for emulator-compatible secret loading.
# The emulator keeps recommendations and assignment drafting deterministic. If a real key is
# supplied here, authenticated read-aloud requests use OpenAI TTS; otherwise they use browser speech.
OPENAI_API_KEY=
STUDENT_PIN_PEPPER=
